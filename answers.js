// 1. Select an element with the id "header"
const header = document.getElementById("header");

// 2. Add a new class to an element
// header.classList.add('highlight');

// 3. Check if a div with the id "content" is empty
// const content = document.getElementById('content');
// const isContentEmpty = !content.hasChildNodes();
// console.log('Is content empty?', isContentEmpty);

// 4. Remove an element from the DOM
// Example: removing the content div
// content.remove();

// 5. Change the background color of the element with id "content" to green
// content.style.backgroundColor = 'green';

// 6. Add a role attribute with the value "button" to a div with the id "myDiv"
// const myDiv = document.getElementById('myDiv');
// myDiv.setAttribute('role', 'button');

// 7. Append a new child element to an existing element
// const newChild = document.createElement('div');
// newChild.textContent = 'I am a new child element';
// header.appendChild(newChild);

// 8. Remove all paragraph elements from the document
// const paragraphs = document.querySelectorAll('p');
// paragraphs.forEach(p => p.remove());

// 9. Change the text content of an element
// header.textContent = 'Updated Header Text';

// 10. Create a new div element
// const newDiv = document.createElement('div');
// newDiv.textContent = 'Newly created div';
// document.body.appendChild(newDiv);

// 11. Add a click event listener to the button that changes the color of the div with id "colorBox" to blue
// const colorBox = document.getElementById('colorBox');
// const button = document.getElementById('button');
// button.addEventListener('click', () => {
//     colorBox.style.backgroundColor = 'blue';
// });

// 12. Add a mouseover event listener to the div with id "hoverBox" that changes its text content to "Mouse is over me!"
// const hoverBox = document.getElementById('hoverBox');
// hoverBox.addEventListener('mouseover', () => {
//     hoverBox.textContent = 'Mouse is over me!';
// });

// 13. Add a keydown event listener to the input element that displays the current input value in the paragraph with id "output"
// const input = document.getElementById('input');
// const output = document.getElementById('output');
// input.addEventListener('keydown', () => {
//     output.textContent = input.value;
// });

//Question 14: move the h3 content in the content box to the center of the box using css properties